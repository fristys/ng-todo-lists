import { ShowCompletedPipe } from './show-completed.pipe';

describe('ShowCompletedPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowCompletedPipe();
    expect(pipe).toBeTruthy();
  });
});
